spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3308/orderdb?useSSL=false&serverTimezone=UTC

server:
  port: 19091

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/ # Eureka Client 와 통신할 Url을 지정한다.
